// import Header from "../components/Header";

export default function IndexPage() {
    return (
        <>
            {/* Habia un problema, se tiene que importar y renderizar el componente <Header /> por cada pagina web, para no realizar esto se tiene que definir un layout */}
            {/* <Header /> */}

            {/* Define el contenido que se mostrara en la pagina web, aparte de la cabecera */}
            <h1>Inicio</h1>

        </>
    )
}
